kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane

  - role: worker

  - role: worker

  - role: worker


#kind create cluster --name dev-cluster --config kind-config.yaml -- Create Cluster command
#kind delete cluster --name dev-cluster -- delete Cluster command 
